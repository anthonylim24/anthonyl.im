# anthonyl.im

## Codex Cloud

Use `bash .codex/setup.sh` as the Codex cloud setup script and `bash .codex/maintenance.sh` as the cache maintenance script. See [docs/codex-cloud.md](docs/codex-cloud.md) for the full environment setup, dev-server commands, and validation checks.
