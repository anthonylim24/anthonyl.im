import { TripEditRedirect } from "./KoreaRedirects"

/** The editor now lives on the trip document. Keep this export for tests and old links. */
export function TripDetail() {
  return <TripEditRedirect />
}
